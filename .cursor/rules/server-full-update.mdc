---
description: Append production deploy steps when deployment is discussed or when shipping repo changes (API/web/prisma/shared). User expects server update commands attached whenever code is updated for the server.
alwaysApply: true
---

# Server full update (append when relevant)

The maintainer wants **how to update production** attached whenever it helps them ship work—not only when they literally say “deploy.”

**Always end the reply** with a short section titled **Server full update (production)** (commands below) when **any** of these apply:

- The user asks about **deploying**, **production**, **server**, **git pull**, **PM2**, **migrations**, **Nginx**, or **hosting** this repo.
- You **implemented or changed** anything that affects what runs on the server: `apps/api`, `apps/web`, `packages/shared`, **Prisma schema/migrations**, PM2 ecosystem, or env vars those apps read.

**Skip the block** only for purely local topics with **no** production path (e.g. editor settings, one-off local-only scripts, docs-only discussion with no code changes to ship)—or when the user clearly says they are not deploying.

When you append the block, **adjust the path** if their deploy directory is not `/var/www/PrizeByRadissonBern`.

```bash
cd /var/www/PrizeByRadissonBern
git pull
npm ci
npm run build -w @housekeeping/shared
npm run build -w @housekeeping/api
npm run build -w @housekeeping/web
cd apps/api && npx prisma migrate deploy && cd ../..
pm2 restart ecosystem.config.cjs
```

Optional (only when you need refreshed seed data: request types, rooms, demo users):

```bash
cd /var/www/PrizeByRadissonBern/apps/api && npm run prisma:seed && cd ../..
```

Notes to mention briefly when relevant:

- **PM2 “Cannot find module … dist/main.js”** — the API was not built on the server. Run `npm run build -w @housekeeping/api` (creates `apps/api/dist/`; it is not in git).
- **`prisma:seed`** can change seed-managed data; skip unless you intend to refresh it.
- If **`NEXT_PUBLIC_API_URL`** or **`WEB_ORIGIN`** changed, set env files first, then rebuild web (and API if needed) before `pm2 restart`.
- Smoke test on the server: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3101/api/v1/auth/me` and `curl` to `3100`.

Full detail: [SERVER-RUNBOOK.md](SERVER-RUNBOOK.md), [DEPLOY-PM2.md](DEPLOY-PM2.md).
